<div class="home">
  <h2 class="text-center">
    <strong>ようこそJIMOTOへ！！</strong><br>
    <strong>このサイトはあなたの地元の魅力を発信していくサイトです。</strong><br>
    <strong>そこにしかない場所や珍しいところまで、どんどん投稿してください！</strong><br>
  </h2>
</div>
<h3 class="title">New Place Post!!</h3>
<div class="top">
  <div class="row mt-5 mx-5">
    <% @places.each do |place| %>
    <div class="mx-auto">
        <p>
          <%= link_to place_path(place.id) do %>
            <%= attachment_image_tag(place, :image, :fill, 250, 200, format: 'jpeg', fallback: "no_image.jpg") %>
          <% end %>
        </p>
        <p>観光地名:
          <%= link_to place_path(place.id) do %>
            <%= place.name %>
          <% end %>
        </p>
        <p>住所:
          <%= place.address %>
        </p>
        <p>投稿ユーザー:
          <% if user_signed_in? %>
            <%= link_to user_path(place.user) do %>
              <%= place.user.name %>
            <% end %>
          <% else %>
            <%= place.user.name %>
          <% end %>
        </p>
        <p>♥<%= place.likes.count %></p>
        <p><%= "コメント#{place.place_comments.count} 件" %></p>
    </div>
      <% end %>
    <%= paginate @place_all %>
  </div>
</div>
